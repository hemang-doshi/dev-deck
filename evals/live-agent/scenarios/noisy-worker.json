{
  "id": "noisy-worker",
  "description": "The stack remains healthy while the worker emits deterministic high-volume benign logs and periodic warnings.",
  "fixture": "complex-saas-stack",
  "setupScenario": "noisy-worker",
  "goal": "Start the stack, identify the worker warning signal, avoid false failed-service claims, verify final health, and stop cleanly.",
  "expected": {
    "warningContains": "queue latency above threshold",
    "failedService": null,
    "finalHealthContains": "running"
  },
  "environment": {},
  "limits": {
    "maxTurns": 8,
    "maxToolCalls": 16,
    "timeoutMs": 240000
  }
}
