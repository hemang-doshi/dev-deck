{
  "id": "api-crash",
  "description": "The API becomes ready, serves briefly, then crashes with deterministic runtime evidence.",
  "fixture": "complex-saas-stack",
  "setupScenario": "api-crash-after-start",
  "goal": "Start the stack, observe the API crash, recover with a targeted restart, verify final health, and stop cleanly.",
  "expected": {
    "failedService": "api",
    "rootCauseContains": "simulated crash after startup",
    "recoveryActionContains": "restart",
    "finalHealthContains": "running"
  },
  "environment": {},
  "limits": {
    "maxTurns": 10,
    "maxToolCalls": 20,
    "timeoutMs": 240000
  }
}
