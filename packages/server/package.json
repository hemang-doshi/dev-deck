{
  "name": "@devdeck/server",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm --prefix ../.. run build --workspace @devdeck/core && tsc -p tsconfig.json",
    "test": "npm --prefix ../.. run build --workspace @devdeck/core && vitest run",
    "lint": "npm --prefix ../.. run build --workspace @devdeck/core && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@devdeck/core": "1.3.0",
    "ws": "^8.18.3"
  }
}
